import { EvidenceView } from "@/components/evidence/evidence-view";

export const metadata = {
  title: "Evidence — DPDPOS",
  description: "Evidence vault with approval workflow",
};

export default function EvidencePage() {
  return <EvidenceView />;
}
