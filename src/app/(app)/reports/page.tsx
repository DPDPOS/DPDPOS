import { ReportsView } from "@/components/reports/reports-view";

export const metadata = {
  title: "Reports — DPDPOS",
  description: "Board-ready compliance reports",
};

export default function ReportsPage() {
  return <ReportsView />;
}
