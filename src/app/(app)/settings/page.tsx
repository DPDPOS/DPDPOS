import { SettingsView } from "@/components/settings/settings-view";

export const metadata = {
  title: "Settings",
  description: "Organization profile and security",
};

export default function SettingsPage() {
  return <SettingsView />;
}
